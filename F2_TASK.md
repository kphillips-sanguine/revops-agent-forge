Read PLAN.md and FRONTEND_PHASES.md in this directory. Execute Phase F2 from FRONTEND_PHASES.md.

Phase F1 is already complete — the React scaffold, app shell, routing, types, and stores are built in `agent-platform-ui/`.

Build on top of the existing code. Do NOT re-scaffold or overwrite existing files unless you need to modify them.

Do NOT ask questions — make decisions and build. Be thorough and complete.

After creating all files, run `npm run build` to verify zero errors. Fix any errors before finishing.

When completely finished and verified working, run this exact command:
```
openclaw system event --text "F2 Complete: Dashboard with stat cards, agent list with filtering/sorting, mock data, status badges all built. npm run build verified clean." --mode now
```
