Read PLAN.md and FRONTEND_PHASES.md in this directory. Execute Phase F1 from FRONTEND_PHASES.md.

Build everything inside the `agent-platform-ui/` subdirectory.

Do NOT ask questions — make decisions and build. Be thorough and complete.

After creating all files, run `npm run dev` to verify the app starts without errors. Fix any errors before finishing.

When completely finished and verified working, run this exact command:
```
openclaw system event --text "F1 Complete: React scaffold, app shell, routing, types all built. npm run dev verified working." --mode now
```
